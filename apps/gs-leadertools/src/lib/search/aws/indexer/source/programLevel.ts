/**
 * ProgramLevel interface represents a level in a program.
 */
export interface ProgramLevel {
  /**
   * The unique name of the program level.
   */
  name: string;

  /**
   * The order of the program level in the sequence of levels.
   */
  order: number;
}
